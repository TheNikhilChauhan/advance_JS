// primitive data type: booleans, null, undefined

let isTrue = 1;

//falsy values
/* 
false
null => it can be explicitly set by a developer to represent intensional absence of value
undefined => if we declare a value but doesn't assign any value then it is undefined
0
-0
NaN
"", '', ``, (empty quotes)
*/

let isProgrammingLanguage = true;
let isItHard = false;
let favNum = 7;
console.log(favNum + undefined); //NaN
